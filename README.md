# pet-emotion-model-training
cat and dog model training using  CNN(efficient B0 )to detect emotion of pet and a sensor data  model using random forest  algorithm.
the effiency of dog dataset is 92% and cat dataset is 98%.
by combinmig both sensors and camera dataset we can conclude whether the pet is healthy or not which popup the alert notification
